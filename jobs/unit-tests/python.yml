unit-tests:
  stage: unit-tests
  allow_failure: true
  image:
    name: python:latest
  artifacts:
    expire_in: 1 day
    paths:
      - coverage.xml
  script:
    - pip install -r requirements.txt
    - python3 -m pytest --cov --cov-report=xml:coverage.xml
